<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- ইন্টারনেট — Firebase/Ads/RevenueCat নিজেরাও merge করে দেয়, তবে
         এক্সপ্লিসিট রাখা নিরাপদ -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- রিমাইন্ডার নোটিফিকেশনের জন্য (Android 13 / API 33+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- এক্সাক্ট-টাইম নোটিফিকেশন শিডিউল করার জন্য (reminder alarms) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- ভয়েস নোট রেকর্ডারের জন্য -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- অ্যাপ লক (বায়োমেট্রিক/ফিঙ্গারপ্রিন্ট) এর জন্য -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- ব্যাকআপ/এক্সপোর্ট ফাইল পড়া-লেখার জন্য (Android 12 ও নিচে;
         Android 13+ এ scoped storage/SAF ব্যবহার হয় বলে প্রভাব সীমিত,
         কিন্তু পুরনো ডিভাইস সাপোর্টের জন্য রাখা হলো) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:label="notenext"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
